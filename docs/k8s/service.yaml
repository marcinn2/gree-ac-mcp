apiVersion: v1
kind: Service
metadata:
  name: gree-ac-mcp-server
  namespace: gree-ac
  labels:
    app.kubernetes.io/name: gree-ac-mcp-server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gree-ac-mcp-server
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
